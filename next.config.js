/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // experimental: {
  //   serverActions: true,
  // },
  // Uncomment if using images from external sources
  // images: {
  //   domains: ['your-domain.com'],
  // },
};

module.exports = nextConfig;
